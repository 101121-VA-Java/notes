import { ExcitedPipePipe } from './excited-pipe.pipe';

describe('ExcitedPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ExcitedPipePipe();
    expect(pipe).toBeTruthy();
  });
});
